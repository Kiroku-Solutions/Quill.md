# Package Managers
package-lock.json
pnpm-lock.yaml
yarn.lock
bun.lock
bun.lockb

# Miscellaneous
/static/
/coverage/

# Markdown docs that embed TypeScript code blocks. The tailwindcss Prettier
# plugin tries to parse TS-looking identifiers as Tailwind classes and
# chokes. These files are hand-formatted (tables, ASCII alignment, etc.) —
# consistent with the pre-existing lint warning on docs/ers.md.
docs/current-project-status.md
